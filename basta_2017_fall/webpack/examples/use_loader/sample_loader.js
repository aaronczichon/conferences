module.exports = function(source) {
    const newSource = doTransformSource(source);

    return newSource;
}

function doTransformSource(source) {
    // Do something with the source
    return source;
}